# Example environment variables for Prompt Assistant
# Copy this file to `.env` and fill in the real values. DO NOT commit your `.env` file.

# OpenAI keys can be found at https://platform.openai.com/account/api-keys
OPENAI_API_KEY=

# Google Gemini keys can be found at https://aistudio.google.com/app/api-keys
GEMINI_API_KEY=

# Copilot CLI can be accessed with a GitHub token.
# Get your token with `gh auth token` and paste it here
GH_TOKEN=

# Claude code requires a mapped directory to reuse json files
# This should be a path like /home/user/claude_data
CLAUDE_DATA_DIR=

# Port the app listens on inside the container (default 8080)
PORT=8080

# Host the app binds to (default 0.0.0.0)
HOST=0.0.0.0
